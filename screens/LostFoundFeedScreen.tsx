import { View, StyleSheet } from "react-native";

function LostFoundFeedScreen(props) {
  return <View style={styles.container}></View>;
}

export default LostFoundFeedScreen;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "red",
  },
});
